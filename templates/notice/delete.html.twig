{% include 'default/_form.html.twig' with {
    page_title: 'title.notice_delete'|trans({'%id%': notice.id}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'notice_index',
} %}